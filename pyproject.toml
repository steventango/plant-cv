[project]
name = "plant-cv"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.uv.workspace]
members = [
    "api/grounding_dino",
    "api/segment_anything",
]
