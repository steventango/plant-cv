services:
  embeddings:
    build:
      context: api/embeddings
      dockerfile: Dockerfile
    env_file: .env
    networks:
      - plant-network
    privileged: true
    restart: unless-stopped
    volumes:
      - ./api/embeddings:/app
      - ~/.cache:/home/docker/.cache:cached
    ports:
      - "8803:8803"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities:
                - gpu

  grounding-dino:
    build:
      context: api/grounding_dino
      dockerfile: Dockerfile
    env_file: .env
    networks:
      - plant-network
    privileged: true
    restart: unless-stopped
    volumes:
      - ./api/grounding_dino:/app
      - ~/.cache:/home/docker/.cache:cached
    ports:
      - "8801:8801"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities:
                - gpu

  segment-anything:
    build:
      context: api/segment_anything
      dockerfile: Dockerfile
    env_file: .env
    networks:
      - plant-network
    privileged: true
    restart: unless-stopped
    volumes:
      - ./api/segment_anything:/app
      - ~/.cache:/home/docker/.cache:cached
    ports:
      - "8802:8802"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities:
                - gpu

  pipeline:
    build:
      context: api/pipeline
      dockerfile: Dockerfile
    networks:
      - plant-network
    restart: unless-stopped
    ports:
      - "8800:8800"
    volumes:
      - ./api/pipeline:/app
      - /data/plant-rl:/data
    depends_on:
      - embeddings
      - grounding-dino
      - segment-anything

networks:
  plant-network:
    name: plant-network
